export const NotificationStyle = (theme) => ({
    notificationContainer: {
        width: '50%',
        height: '1em',
        '& > p':{
            fontSize: '1em',
            color: 'red',
        }
    }
});